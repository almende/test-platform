{
  "name": "assetC",
  "version": "1.0.0",
  "description": "Sample vAsset, forwarding REST calls to asset B, but trying directly around the reverseProxy",
  "main": "assetC.js",
  "scripts": {
    "start": "npm -g install; node assetC.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "author": "Luis F.M. <luis@almende.org>",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "request": "^2.86.0"
  },
  "devDependencies": {
    "eslint": "^4.15.0"
  }
}
